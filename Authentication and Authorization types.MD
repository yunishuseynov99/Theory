# 🔹 **Что такое Authorization**

**Authorization** — это процесс **проверки прав пользователя**, то есть: *“Может ли этот пользователь делать это действие?”*

* Authentication (логин/пароль) → **кто ты**
* Authorization → **что тебе разрешено**

Например:

* Alice вошла в систему → authentication
* Проверяем, может ли Alice удалить пост → authorization

---

# 🔹 **Основные способы/механизмы Authorization**

## 1️⃣ **Role-based Access Control (RBAC)**

**Идея:** пользователю присваивается **роль**, роли определяют разрешения.

Пример:

| Роль   | Разрешения          |
| ------ | ------------------- |
| Admin  | Read, Write, Delete |
| Editor | Read, Write         |
| Viewer | Read                |

**Как работает в C#:**

```csharp
[Authorize(Roles = "Admin")]
public IActionResult DeletePost(int id)
{
    // Только админ может удалить
}
```

---

## 2️⃣ **Claim-based Authorization (CBA)**

**Идея:** пользователь имеет **claims** (утверждения), например `"CanEditPosts": true`, `"Department": "HR"`.

* Более гибко, чем роли.
* Используется в JWT, IdentityServer, ASP.NET Core.

Пример в C#:

```csharp
[Authorize(Policy = "CanEditPosts")]
public IActionResult Edit(int id)
{
}
```

И политика задаётся в `Program.cs`:

```csharp
builder.Services.AddAuthorization(options =>
{
    options.AddPolicy("CanEditPosts", policy =>
        policy.RequireClaim("CanEditPosts", "true"));
});
```

---

## 3️⃣ **Attribute-based / Policy-based Authorization**

* Атрибуты (`[Authorize]`) + политики (Policy)
* Политика может проверять **роль, claim, время доступа, IP, статус пользователя**.

Пример:

```csharp
options.AddPolicy("HRPolicy", policy =>
    policy.RequireRole("HR")
          .RequireClaim("EmploymentStatus", "Active"));
```

---

## 4️⃣ **Resource-based Authorization**

* Проверка доступа к конкретному объекту/ресурсу.
* Например: может ли пользователь редактировать **именно этот пост**.

```csharp
public async Task<bool> CanEditPost(User user, Post post)
{
    return post.OwnerId == user.Id || user.IsAdmin;
}
```

* Используется вместе с RBAC/Claims.

---

## 5️⃣ **Permission-based Access Control**

* Вместо ролей, напрямую назначаем **конкретные разрешения** пользователю.
* Пример: `"Post.Edit"`, `"Post.Delete"`.
* Гибко для сложных систем.

---

## 6️⃣ **Attribute/Filter-based Authorization в ASP.NET**

* `[Authorize]` для классов и методов контроллеров.
* `[AllowAnonymous]` для исключений.
* Можно комбинировать: Roles + Policies.

---

# 🔹 **Кратко: RBAC vs Claims vs Policy vs Resource**

| Подход   | Гибкость      | Пример использования                   |
| -------- | ------------- | -------------------------------------- |
| RBAC     | Низкая        | Admin/Editor/Viewer                    |
| Claims   | Средняя       | “CanEditPosts = true”                  |
| Policy   | Высокая       | Проверка claim + роль + условие        |
| Resource | Очень высокая | Проверка доступа к конкретному объекту |

---



---

# 🔹 **1. Basic Authentication**

**Как работает:**
Клиент отправляет логин и пароль (обычно Base64) в `Authorization` header при каждом запросе.

**Пример header:**

```
Authorization: Basic dXNlcjpwYXNzd29yZA==
```

**Плюсы:**

* Простая реализация.
* Поддерживается везде.

**Минусы:**

* Нужен HTTPS, иначе пароль видно в сети.
* Нет механизма «разлогиниться».
* Нельзя легко отозвать доступ.

**Где используется:**
В простых API, внутренних сервисах, временных решениях.

---

# 🔹 **2. Cookie‑based Authentication (Session)**

**Как работает:**

* Пользователь логинится → сервер создаёт сессию в памяти/Redis/DB → отдает `Set-Cookie`.
* На каждом запросе браузер автоматически отправляет cookie.

**Плюсы:**

* Простая интеграция с веб‑сайтами.
* Можно хранить много данных в серверной сессии.

**Минусы:**

* Требуется sticky‑sessions или shared‑storage.
* Неудобно для мобильных приложений и микросервисов.

**Где используется:**
Классические сайты, старые ASP.NET MVC, PHP, Django.

---

# 🔹 **3. Token‑based Auth (JWT)**

**Как работает:**

* Сервер выдает токен (обычно JWT).
* Клиент отправляет токен в заголовке:

```
Authorization: Bearer eyJhbGciOiJIUzI1...
```

**Плюсы:**

* Stateless — не нужно хранить сессию на сервере.
* Удобно для SPA, Mobile, microservices.
* Можно хранить claims: роли, права, ID.

**Минусы:**

* Токен нельзя «убить» до истечения срока без отдельного blacklist.
* Нужно защищать от XSS (если хранится в LocalStorage).

**Где используется:**
Angular/React SPA, .NET WebAPI, Go, Node.

---

# 🔹 **4. OAuth 2.0**

**Как работает:**
Пользователь не вводит пароль на стороннем сайте. Сайт перенаправляет его в Google/Facebook/etc → получает `access_token`.

**Плюсы:**

* Единственный вход (SSO).
* Нет хранения паролей.

**Минусы:**

* Сложнее протокол.
* Нужен отдельный auth‑provider.

**Где используется:**
Login with Google/Facebook/GitHub, корпоративные SSO.

---

# 🔹 **5. OpenID Connect (OIDC)**

Расширение OAuth 2.0, которое добавляет подтверждение личности.

**Преимущество:**
Вы получаете не только access_token, но и `id_token` с user info.

**Используется:**
IdentityServer, Keycloak, Azure AD, Auth0.

---

# 🔹 **6. Client Certificates (mTLS)**

**Как работает:**
У клиента есть сертификат, сервер проверяет его при TLS handshake.

**Плюсы:**

* Очень безопасно.
* Пароли не нужны.

**Минусы:**

* Непростое управление сертификатами.
* Сложно для обычных пользователей.

**Где используется:**
B2B API, банки, гос‑сервисы.

---

# 🔹 **7. API Key**

**Как работает:**
Сервис получает ключ:

```
x-api-key: 39f4f12a-...
```

**Плюсы:**

* Просто.
* Хорошо подходит для service‑to‑service.

**Минусы:**

* Нет user identity.
* Нужно защищать ключ.

---

# 🔹 **8. Windows/AD Authentication (Kerberos)**

**Как работает:**
SSO внутри корпоративной сети (NTLM, Kerberos).

**Где используется:**
Корпоративные приложения, SharePoint, on‑premise системы.

---

# 🔹 **9. Passwordless**

**Типы:**

* Magic link (email login).
* TOTP (Google Authenticator).
* FIDO2 / WebAuthn (FaceID, fingerprint).

**Плюсы:**
Пароль не нужен, нечего украсть.

---

# 🔹 **10. Hardware Tokens**

**Например:**

* YubiKey
* SmartCard

Используются в банковских и государственных системах.

---

# ❗ **Когда что выбирать**

| Ситуация               | Рекомендуем           |
| ---------------------- | --------------------- |
| SPA + Backend          | JWT / OIDC            |
| Микросервисы           | JWT + mTLS            |
| Корпоративная сеть     | Kerberos, AD          |
| Публичный API          | API Key + Rate limit  |
| Банк, гос              | mTLS + Hardware token |
| Внутренний сервис      | Basic / JWT           |
| OAuth Login с соцсетей | OAuth2 / OIDC         |

---
