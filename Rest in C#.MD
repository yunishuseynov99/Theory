**REST (Representational State Transfer)** — это архитектурный стиль проектирования распределённых систем, особенно веб-API, основанный на принципах HTTP.
Важно понимать: **REST — не протокол и не стандарт**, а набор ограничений (constraints), соблюдение которых делает систему RESTful.

---

# 🚀 Senior-level explanation

## 🔹 Основные принципы REST

### 1. **Client–Server**

Разделение ответственности:

* Клиент знает *как* отображать и использовать данные.
* Сервер знает *как* хранить и обрабатывать данные.

Это обеспечивает независимую масштабируемость и модульность.

---

### 2. **Stateless**

Каждый запрос **самодостаточен**:
Сервер **не хранит состояние клиента между запросами**.
Если тебе нужен контекст (auth, filters, locale) — передавай его **в каждом запросе** (обычно через headers или token).

---

### 3. **Uniform Interface**

Единообразный контракт между клиентом и сервером.

Ключевые элементы:

* **Ресурсы** (users, orders) → nouns, не actions
* **URL** описывает *что*, а не *что сделать*:
  `POST /orders` → создать заказ,
  `PUT /orders/123` → обновить заказ.
* **HTTP-методы** имеют семантику:

  * GET — read
  * POST — create
  * PUT — update (replace)
  * PATCH — partial update
  * DELETE — remove

---

### 4. **Resource-based**

REST работает с *ресурсами*, а не *операциями*.
❌ `/approveLoan?id=10`
✔️ `/loans/10/approve` (и то это уже не чистый REST — лучше событие)

---

### 5. **Use of HTTP semantics**

REST **правильно использует** HTTP:

* **Status codes:**
  200 OK, 201 Created, 204 No Content,
  400 Bad Request, 404 Not Found, 409 Conflict, 500 Internal Server Error
* **Caching:**
  `ETag`, `304 Not Modified`
* **Content Negotiation:**
  `Accept: application/json`

---

### 6. **Layered System**

Клиент может не знать, общается он с сервером напрямую или через прокси / gateway.

---

### (Опционально) 7. **HATEOAS**

Ответ API содержит ссылки на возможные действия.
В реальных проектах почти никто не делает.

---

## 🔹 Что **не** является REST

❌ REST ≠ JSON
❌ REST ≠ CRUD
❌ REST ≠ «у меня есть HTTP API»
❌ REST ≠ Swagger

---

## 🔹 Почему REST популярен

* Простота → HTTP everywhere
* Лёгкая интеграция между сервисами
* Кэширование из коробки
* Масштабируемость
* Независимость клиента/сервера

---

## 🔹 Когда REST плохо подходит

| Ситуация              | Лучше использовать  |
| --------------------- | ------------------- |
| Много мелких запросов | GraphQL             |
| Realtime              | WebSockets, SignalR |
| Тяжёлые транзакции    | gRPC, CQRS          |
| Strong typing         | gRPC                |

---

## 🔹 Практические советы Senior-уровня

### ✔ Думай ресурсами, не методами

Плохо:

```
POST /calculateInterest
```

Хорошо:

```
GET /loans/123/interest
```

---

### ✔ Используй правильные HTTP-коды

Не отвечай **200 OK** на всё.

---

### ✔ Делай версионирование API

Наиболее практично:

```
/api/v1/loans
```

---

### ✔ Безопасность

* JWT / OAuth2
* CORS
* Rate limiting

---

### ✔ Не передавай бизнес-ошибки в 200

Плохо:

```json
{ "success": false, "reason": "Not enough balance" }
```

Хорошо:

```
409 Conflict
```

---

## 🔹 Итоговое краткое определение (Senior-level)

> **REST** — это архитектурный стиль, где сервер предоставляет доступ к ресурсам через стандартизованный HTTP-интерфейс, соблюдая принципы stateless, единообразного контракта, чёткой семантики методов и использования стандартных HTTP-механизмов (коды, кэширование, контент-негация). Если ты нарушаешь эти правила — это может быть удобный HTTP API, но оно уже не RESTful.

---

Ниже — **глубокое, senior-level объяснение каждого способа передачи параметров в REST API**, с разбором:

* **Что это такое технически**
* **Когда использовать**
* **Когда НЕ использовать (антипаттерны)**
* **Примеры правильные / неправильные**
* **Особенности ASP.NET Core (`[FromBody]`, `[FromQuery]`, `[FromRoute]`, …)**

---

# 🔥 Главный принцип

**Место передачи параметра = его смысл.**

❌ Если ты просто “куда-то положишь” параметр и оно работает — значит ты пишешь *HTTP-RPC, а не REST*.

✔ REST — это **контракт**. Клиент может предсказать, что где лежит.

---

# 🌍 1. PATH / ROUTE parameters (`[FromRoute]`)

## Что это

Часть URL, обозначающая **конкретный ресурс**, без которого запрос **не имеет смысла**.

```
GET /users/123
```

## Когда использовать ✔

* Идентификатор ресурса (ID, GUID, slug)
* Иерархия ресурсов:

```
GET /users/123/orders/456
```

* Команды, привязанные к сущности:

```
POST /loans/200/approve
```

## Когда НЕ использовать ❌

* Фильтры (`/users/active` → должно быть `/users?status=active`)
* Множественные ID (`/users/1,2,3`) — это query
* Состояние (`/loans/123?status=active` — не надо `/loans/123/active`)

## ASP.NET Core

```csharp
[HttpGet("users/{id}")]
public UserDto Get([FromRoute] int id)
```

🌟 `[FromRoute]` **необязателен** — MVC сам понимает.

---

# 🔎 2. Query parameters (`[FromQuery]`)

## Что это

Параметры в URL **после `?`**, НЕ влияющие на *идентификацию ресурса*, а только на *выборку или поведение*.

```
GET /users?status=active&limit=10
```

## Когда использовать ✔

* Фильтры (`?status=active`)
* Сортировка (`?sort=date`)
* Пагинация (`?page=2&limit=50`)
* Поля ответа (`?fields=id,name`)
* Доп. опции поведения (`?includeTotalCount=true`)

## Когда НЕ использовать ❌

* Идентификатор ресурса (`GET /users?id=10` — ***плохо***)
* Пароли / токены (`?token=` → утечёт в логи)
* Большие объекты (URL ограничен ~2KB)

## ASP.NET Core

```csharp
public async Task Get([FromQuery] UserFilter filter)
```

👉 Можно биндингом в объект, не только в примитивы.

---

# 📦 3. Body (`[FromBody]`)

## Что это

**Тело HTTP-запроса**, обычно JSON. Это **представление ресурса (representation)**.

## Когда использовать ✔

* **POST** — создание ресурса
* **PUT** — полная замена ресурса
* **PATCH** — частичное обновление
* Сложные DTO

```
POST /loans
{
  "amount": 10000,
  "term": 12
}
```

## Когда НЕ использовать ❌

* В **GET** — стандартом НЕ предусмотрено
* Для фильтров → это RPC стиль
* Для команд, которые по смыслу не создают ресурсы

---

## ASP.NET Core

```csharp
public Task Create([FromBody] CreateLoanRequest request);
```

🟥 **Только один `[FromBody]` в запросе!**
(Body нельзя сплитить — аналог "один InputStream")

---

# 🏷 4. Header (`[FromHeader]`)

## Что это

Мета-информация, НЕ относящаяся к ресурсам.

Например:

```
Authorization: Bearer <token>
Accept-Language: en-US
```

## Когда использовать ✔

* Авторизация (JWT, API key)
* Локализация (`Accept-Language`)
* Трассировка (`X-Request-Id`)
* Кэш (`If-None-Match`, `Last-Modified`)

## Когда НЕ использовать ❌

* Бизнес-данные
* Фильтры
* Данные, которые нужно логировать вместе с URL

## ASP.NET Core

```csharp
public Task Get([FromHeader(Name="X-Request-Id")] string id)
```

📌 Обычно **не пишут вручную**, middleware сам читает.

---

# 🍪 5. Cookies (`[FromCookie]`)

## Что это

Хранилище **на стороне клиента**, привязанное к домену.

## Когда использовать ✔

* Web-приложения с браузером
* Session ID
* Anti-CSRF tokens

## Когда НЕ использовать ❌

* Public REST API
* Mobile / backend calls
* Передача чувствительных данных без `HttpOnly` + `Secure`

## ASP.NET Core

```csharp
var userId = Request.Cookies["uid"];
```

🛑 В API лучше **не использовать cookies вообще**.

---

# 📡 6. Form-data (`multipart/form-data`)

## Что это

Форма, которая может содержать файлы и поля.

## Когда использовать ✔

* Загрузка файлов (`profile-picture.jpg`)
* Отправка формы из браузера

## Когда НЕ использовать ❌

* JSON DTO без файлов
* API, который вызывается не браузером

---

# 📊 7. Matrix parameters (редкость)

```
/cars;color=red;year=2020
```

Не используйте — это не поддерживается большинством клиентов.

---

# ⚠️ Антипаттерны

❌ Передавать JWT в query:

```
GET /users?token=ASD123
```

❌ Передавать большое DTO в query
❌ Использовать body в GET
❌ `POST /calcInterest?id=10` — RPC, не REST
❌ Передавать фильтры в body:

```
POST /users/filter { status: "active" }
```

---

# 🧠 Правило Senior-разработчика (золотое)

| Тип параметра               | Правильное место |
| --------------------------- | ---------------- |
| ID ресурса                  | PATH             |
| Фильтры, поиск              | Query            |
| Опции поведения             | Query            |
| Представление ресурса (DTO) | Body             |
| Токен, meta                 | Header           |
| Web-session                 | Cookies          |
| Файлы                       | Multipart        |

---

# 🔥 **Коротко: когда нельзя**

| Место  | Нельзя                         |
| ------ | ------------------------------ |
| Body   | GET, DELETE                    |
| Query  | Secret tokens, большие объекты |
| Route  | Фильтры, состояние             |
| Header | Бизнес-информация              |

---

# 💎 Финальный совет старшего разработчика

> **REST — это не “куда клиент положил данные”, а гарантия, что другой клиент сможет сделать запрос, не читая документацию.**
> Если параметр *меняет то, какой ресурс ты запрашиваешь* — он в PATH.
> Если *меняет, что вернётся* — в QUERY.
> Если это *представление объекта* — в BODY.
> Всё остальное — HEADER.

---
