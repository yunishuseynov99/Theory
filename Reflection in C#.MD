Reflection в C# — это механизм, позволяющий **исследовать и взаимодействовать с типами (классами, методами, свойствами и т.д.) во время выполнения**, даже если ты заранее не знаешь, какой это тип.

---

# 🧠 Как понять Reflection на пальцах

Без рефлексии:

```csharp
var person = new Person();
Console.WriteLine(person.Name); // компилятор знает тип
```

С рефлексией:

```csharp
var type = person.GetType(); // получаем информацию о типе в рантайме
var prop = type.GetProperty("Name");
Console.WriteLine(prop.GetValue(person));
```

➡️ **Ты обращаешься к свойству по имени, как будто у тебя динамический язык.**
➡️ Компилятор не знает имя свойства заранее — ты узнаёшь его только в runtime.

---

# 🔧 Что можно делать через Reflection

| Возможность                            | Пример                      |
| -------------------------------------- | --------------------------- |
| Узнать, какие свойства есть у объекта  | сериализация в JSON         |
| Получить атрибуты                      | [Authorize], [JsonProperty] |
| Вызвать метод по имени                 | например, plugin systems    |
| Создавать экземпляры типов динамически | DI контейнеры               |
| Проверить типы в сборке                | загрузка плагинов           |

---

# ⚙️ Где реально используется Reflection

### 1️⃣ JSON сериализация

```csharp
Newtonsoft.Json
System.Text.Json
```

Они смотрят, какие свойства у объекта, и читают их значения.

### 2️⃣ Dependency Injection

```csharp
services.AddTransient<IMyService, MyService>()
```

DI-контейнер через reflection находит конструктор, параметры и вызывает его.

### 3️⃣ ASP.NET MVC / Minimal APIs

Когда ты пишешь:

```csharp
[HttpGet("user/{id}")]
public User Get(int id)
```

Фреймворк через reflection ищет методы с атрибутами `HttpGet`, находит, какие параметры там есть, и вызывает их по запросу.

### 4️⃣ Entity Framework

EF анализирует модель для построения SQL.
Он не знает ничего о твоих классах заранее — он получает их через Reflection.

---

# 🧱 Как это выглядит на низком уровне

### Получить все свойства:

```csharp
var type = typeof(Person);
foreach (var prop in type.GetProperties())
{
    Console.WriteLine(prop.Name);
}
```

### Создать объект без new:

```csharp
var type = Type.GetType("MyApp.Person");
var instance = Activator.CreateInstance(type);
```

### Вызвать метод по имени:

```csharp
var method = type.GetMethod("SayHello");
method.Invoke(instance, null);
```

---

# 📌 Важные моменты

### ❗ Reflection **дорогой**

* Медленнее обычного вызова (иногда **в 50–100 раз**)
* Но 99% фреймворков кешируют результаты — и ты это не чувствуешь

### ❗ Ошибки — **в runtime**

Если ты ошибёшься в имени свойства → упадёшь во время выполнения, а не на этапе компиляции.

### ❗ Иногда нужен доступ к private

Можно вызвать даже приватный метод:

```csharp
var method = type.GetMethod("Init", BindingFlags.NonPublic | BindingFlags.Instance);
```

---

# 🧑‍💻 Как понять, что ты уже использовал Reflection

Если ты:

✔️ Использовал атрибуты
✔️ Вызывал `typeof(...)`
✔️ Писал сериализацию
✔️ Работал с `Assembly.Load`
✔️ Создавал DI контейнеры

➡️ Ты уже сталкивался с Reflection, просто не напрямую.

---

# 🧠 Как мысленно запомнить

**Reflection = “узнать и вызвать всё что угодно, даже если заранее неизвестно”.**
Это как будто C# временно превращается в Python/JS — всё динамическое.

---

# 💡 Простой пример для запоминания

```csharp
public class User {
    public string Name { get; set; }
}

object obj = new User { Name = "Yunis" };

Console.WriteLine(obj.Name); // ❌ не скомпилируется

// но
var prop = obj.GetType().GetProperty("Name");
var value = prop.GetValue(obj);
Console.WriteLine(value); // 👍 работает!
```

---

# 🎯 Когда тебе лично нужно знать Reflection?

Если ты:

* пишешь **инфраструктурный код** (логгер, DI, сериалайзеры, ORM)
* делаешь **плагины и модульные приложения**
* хочешь разбирать чужие сборки
* пишешь свои **кастомные атрибуты**

→ Без Reflection — никуда.

---

**📌 объяснение, как работает `typeof` vs `GetType()`

Отличный вопрос — **typeof** и **GetType()** очень похожи внешне, но **работают принципиально по-разному**.

---

# 🧠 Короткий ответ

|                            | `typeof(SomeClass)`                 | `obj.GetType()`                        |
| -------------------------- | ----------------------------------- | -------------------------------------- |
| Когда определяется         | **во время компиляции**             | **во время выполнения (runtime)**      |
| Требует экземпляр объекта? | ❌ нет                               | ✔️ да                                  |
| Возвращает                 | Тип, который ты указал прямо в коде | Реальный тип объекта в памяти          |
| Можно вызвать от null?     | ✔️ да                               | ❌ нет (будет `NullReferenceException`) |

---

# 🔍 Пример — разница на практике

```csharp
object x = "hello";

Console.WriteLine(typeof(string) == x.GetType()); // true
```

✔️ Оба типа совпадают

Но:

```csharp
object y = 5;

Console.WriteLine(typeof(object) == y.GetType()); // false
```

Потому что:

* `typeof(object)` → мы явно указали object
* `y.GetType()` → настоящий тип в рантайме = `System.Int32`

---

# 🧊 Типично собеседовательный пример

```csharp
class Animal {}
class Dog : Animal {}

Animal a = new Dog();

Console.WriteLine(typeof(Animal));   // Animal
Console.WriteLine(a.GetType());      // Dog
```

➡️ **typeof смотрит на то, что ты написал**
➡️ **GetType() смотрит на то, что реально лежит в памяти**

---

# ⚙ Что происходит "под капотом"

### 🏷 typeof(MyClass)

* Компилятор встраивает ссылку на metadata типа
* Тип становится известен **ещё до запуска программы**
* Это **IL инструкция `ldtoken`**, не вызов метода

### 🔍 obj.GetType()

* Это **виртуальный вызов метода** у базового класса `System.Object`
* CLR смотрит в object header, читает pointer на MethodTable -> TypeHandle
* То есть это runtime lookup

---

# ❗ Важные отличия

### 1️⃣ `typeof()` работает даже без объекта

```csharp
Type t = typeof(DateTime); // нет экземпляра → всё равно работает
```

### 2️⃣ `GetType()` выкинет null exception

```csharp
object o = null;
o.GetType(); // 💥 NullReferenceException
```

### 3️⃣ `typeof` нельзя заменить `GetType`

Ты не можешь сделать:

```csharp
GetType(MyClass) // ❌ не скомпилируется
```

---

# 🔥 Когда что использовать

### Используй `typeof`, когда:

✔ работаешь со статическим API (`Activator.CreateInstance(typeof(Foo))`)
✔ проверяешь тип без объекта
✔ пишешь атрибуты:

```csharp
[JsonConverter(typeof(MyConverter))]
```

### Используй `GetType()`, когда:

✔ у тебя **есть объект**, но компилятор не знает его точный тип
✔ нужно делать runtime-инспекцию (Reflection)
✔ пишешь логгер и хочешь узнать конкретный тип

---

# 💡 Уровень "Сеньор"

`typeof(T)` → всегда один и тот же объект `System.Type`
`someObj.GetType()` → ВСЕГДА возвращает **точный runtime тип**, даже если объект лежит в переменной базового типа

---

# 🚀 Мини-итог

| Сравнение          | typeof | GetType() |
| ------------------ | ------ | --------- |
| Compile-time       | ✔      | ❌         |
| Runtime            | ❌      | ✔         |
| Нужен объект       | ❌      | ✔         |
| Ловит реальный тип | ❌      | ✔         |

---

