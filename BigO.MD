Вот **простое и практичное объяснение**, как **C# разработчику** научиться вычислять **Time Complexity (временную сложность)** и **Space Complexity (память)**, просто глядя на код.

---

# 🧠 Что такое Time & Space Complexity?

* **Time Complexity → сколько операций сделает программа**
* **Space Complexity → сколько доп. памяти (не считая входных данных) ей нужно**

Цели:

* Понять, **насколько медленно растёт программа**, когда растут входные данные (`n`).
* Понять, **сколько лишней памяти выделяет** код.

---

# ⏱ Как считать Time Complexity (время)?

## 1️⃣ Смотри на циклы

| Код                           | Время               |
| ----------------------------- | ------------------- |
| `for (int i = 0; i < n; i++)` | **O(n)**            |
| Два цикла подряд              | **O(n + n) = O(n)** |
| Вложенный цикл                | **O(n²)**           |

### Пример:

```csharp
for (int i = 0; i < n; i++)
    Console.WriteLine(i);

for (int i = 0; i < n; i++)
    Console.WriteLine(i);
```

❗ Два цикла НЕ дают O(2n), а дают **O(n)**
Константы выбрасываются.

---

## 2️⃣ Смотри на вложенность

```csharp
for (int i = 0; i < n; i++)
    for (int j = 0; j < n; j++)
        Console.WriteLine(i + j);
```

➡ **O(n × n) = O(n²)**

---

## 3️⃣ Условие `if` не увеличивает сложность

```csharp
if (x > 10)
   ...
else
   ...
```

❗ `if` — это O(1)

---

## 4️⃣ Рекурсия

* **Обычно = количеству вызовов**

```csharp
int F(int n) => n <= 1 ? 1 : F(n-1) + F(n-2);
```

Это **O(2ⁿ)** → взрывается.

---

## 5️⃣ Использование коллекций

| Операция                    | Сложность                    |
| --------------------------- | ---------------------------- |
| `List.Add()`                | **O(1)** амортизированно     |
| Доступ по индексу `list[i]` | **O(1)**                     |
| `Dictionary.TryGetValue()`  | **O(1)**                     |
| `List.Insert(0, item)`      | **O(n)** (сдвигает элементы) |
| `list.Contains()`           | **O(n)**                     |

---

## 6️⃣ Логарифм появляется, когда делишь пополам

```csharp
while(n > 1)
   n /= 2;
```

➡ **O(log n)**

Это всё бинарный поиск и деревья.

---

# 💾 Как считать Space Complexity (память)?

Считаем **дополнительную память**, не включая входные данные.

---

## 1️⃣ Простые переменные → O(1)

```csharp
int x = 0;
bool flag = false;
```

---

## 2️⃣ Массив на `n` элементов → O(n)

```csharp
var arr = new int[n];
```

---

## 3️⃣ Рекурсия → стек

```csharp
void A(int n) => A(n-1);
```

➡ O(n) памяти (если 1000 вызовов → 1000 стек-фреймов)

---

## 4️⃣ Создаёшь новую коллекцию → считаем

```csharp
List<int> result = new();
for (...) result.Add(...)
```

➡ если добавляешь `n` элементов → **O(n)**

---

# 🔥 Примеры и как считать

---

## ✔ Пример 1

```csharp
int Sum(int[] arr)
{
    int s = 0;
    foreach (var x in arr)
        s += x;
    return s;
}
```

⏱ Time → O(n)
💾 Space → O(1)

---

## ✔ Пример 2

```csharp
var list = new List<int>();
for (int i = 0; i < n; i++)
    for (int j = 0; j < n; j++)
        list.Add(i + j);
```

⏱ **O(n²)**
💾 **O(n²)** → столько элементов хранится

---

## ✔ Пример 3

```csharp
int F(int n)
{
    if (n <= 1) return 1;
    return F(n-1) + F(n-2);
}
```

⏱ Time → **O(2ⁿ)**
💾 Space → **O(n)** стек вызовов

---

# 🧠 5-Шаговый алгоритм для C# разработчика

1. **Ищи циклы**
2. **Ищи вложенность циклов**
3. **Ищи рекурсию**
4. **Проверь коллекции — хранят ли они `n` элементов?**
5. **Игнорируй константы — O(2n + 5) = O(n)**

---

# 🏁 Главное правило

➡ **Сложность = как быстро растёт количество операций / память, когда растёт `n`**
➡ **Не считаем реально операции, считаем масштаб**

---

Конечно! Вот **красивое и компактное резюме Big O** для C# разработчика в виде таблицы:

| Тип операции / структура                      | Временная сложность O(...) | Пространственная сложность O(...) | Комментарий                   |
| --------------------------------------------- | -------------------------- | --------------------------------- | ----------------------------- |
| Простая операция (присвоение, арифметика, if) | O(1)                       | O(1)                              | Константное время и память    |
| Цикл `for` от 0 до n                          | O(n)                       | O(1)                              | Итерации растут с n           |
| Два последовательных цикла                    | O(n + n) → O(n)            | O(1)                              | Константы игнорируем          |
| Вложенный цикл (n × n)                        | O(n²)                      | O(1)                              | Прямоугольная вложенность     |
| Рекурсия (линейная)                           | O(n)                       | O(n)                              | Стек вызовов тоже память      |
| Рекурсия Фибоначчи (без мемоизации)           | O(2ⁿ)                      | O(n)                              | Экспоненциальный рост вызовов |
| Деление пополам (`while(n>1){n/=2}`)          | O(log n)                   | O(1)                              | Бинарный поиск, дерево        |
| List.Add()                                    | O(1) амортизировано        | O(n)                              | Хранит n элементов            |
| List.Insert(0, item)                          | O(n)                       | O(n)                              | Сдвигаются элементы           |
| list.Contains()                               | O(n)                       | O(1)                              | Линейный поиск                |
| Dictionary.TryGetValue()                      | O(1)                       | O(n)                              | Хеш-таблица                   |
| Создание массива на n элементов               | O(n)                       | O(n)                              | Дополнительная память         |

---
